program test;
var x:integer, w:integer;
Begin
    for x:=0 into 5 do
        Begin
            write(x);
            for w:=1 into 2 do
                Begin
                    write(w)
                end;
        end
end.